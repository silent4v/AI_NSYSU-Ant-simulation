#include <iostream>
#include "ant.h"
using namespace std;
int main()
{
   /* Space **tt = create_space();
    bool alive;
    char temp;
    file_write(tt);
    for(p = space_map.begin() ; p != space_map.end() ; p++)
        cout << p->first.x << " " << p->first.y << endl;
    
    delete_var(tt);
    return 0;
}