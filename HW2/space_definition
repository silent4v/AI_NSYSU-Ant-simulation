define:
    TIME_INTERVAL 
    SPACE_INTERVAL
    MIN_X   //max space index of x
    MIN_Y   //min space index of y
    MAX_X   //max space index of x
    MAX_Y   //max space index of y
    
mapping object to number:
    OUTRANGE
    EMPTY
    ANT
    FOOD
    PHEROMONE
    HOME

space 功能：
    create a space 
    update the space(remove dead thing out of space)